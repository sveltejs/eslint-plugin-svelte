<script>
	import { readable, writable } from 'svelte/store';

	readable(false, (set) => true);
	readable(false, (set) => true);

	writable(false, (set) => true);
	writable(false, (set) => true);
</script>
